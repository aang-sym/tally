# AWS Configuration
aws_region   = "ap-southeast-2"
environment  = "dev"
project_name = "tally"

# Email for pipeline error alerts (optional — leave empty to skip)
alert_email = "your-email@example.com"

# API keys are stored in AWS SSM Parameter Store — not here.
# Run infrastructure/scripts/setup_ssm_secrets.sh to populate them.
