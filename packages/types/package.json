{
  "name": "@tally/types",
  "version": "0.1.0",
  "description": "Shared types and schemas for Tally",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@tally/config": "file:../config",
    "typescript": "^5.3.3"
  }
}